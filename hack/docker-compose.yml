version: "3"
services:
  stefan-test:
    image: "nginx:latest"
    ports:
      - "80:80"
    volumes:
      - "./nginx.conf:/etc/nginx/conf.d/default.conf"
      - "./reprepro/repository:/repository/debian"
    restart: always
