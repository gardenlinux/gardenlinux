name: dev
on: [ push, pull_request ]
jobs:
  #  build:
  #  uses: ./.github/workflows/build.yml
  #  with:
  #    version: today
  platform_tests:
    uses: ./.github/workflows/tests.yml
#    needs: build
    permissions:
      id-token: write
