name: dev
on: [ push, pull_request ]
jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      version: today
  gcp_tests:
    needs: build
    permissions:
      id-token: write
    uses: ./.github/workflows/gcp_tests.yml
