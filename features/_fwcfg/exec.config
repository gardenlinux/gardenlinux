#!/usr/bin/env bash

set -eufo pipefail

systemctl enable qemu-fw_cfg-script.service

# just testing to see if the cloud init generator is too slow
mkdir -p /etc/systemd/system-generators
ln -s /dev/null /etc/systemd/system-generators/cloud-init-generator
