description: "OpenStack platform"
type: platform
features:
  include:
  # This set of includes gets auto adjusted depend on if we build as openstack or openstack-metal
  # This works by cloud excluding openstackMetal and metal excluding cloud and openstackCloud
    - openstackCloud
    - openstackMetal
    - server # needed to enforce proper sorting
