description: "cloud functionality (cloud kernel, cloud boot)"
type: element
features:
  include:
    - server
    - _secureboot
fs:
  - dest: /
    type: ext4
disk:
  label: gpt
  boot:
    - mbr
convert:
  format:
    - type: raw
