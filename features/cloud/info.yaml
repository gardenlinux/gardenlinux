description: "cloud functionality (cloud kernel, cloud boot)"
type: 'modifier'
features:
  include:
    - server
fs:
  - dest: /
    type: ext4
disk:
  label: gpt
  boot:
    - mbr
convert:
  format:
    - type: raw
