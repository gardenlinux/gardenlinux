#!/usr/bin/env bash
set -Eeuo pipefail

mkdir -p /usr/share/factory
tar c --exclude="/usr/*" --exclude="/proc/*" --exclude="/sys/*" --exclude="/dev/*" \
    --exclude="/tmp/*" --exclude="/run/*" --exclude "/boot/efi/*" / | \
    tar x -C /usr/share/factory/
