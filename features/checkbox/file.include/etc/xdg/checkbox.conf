[environment]
# Limit stress test duration for hardware validation runs.
# Default upstream values are 7200s (CPU) and ~11000s (memory).
# 60 seconds is sufficient for basic longevity validation.
STRESS_NG_CPU_TIME = 60
STRESS_NG_MEMORY_TIME = 60
