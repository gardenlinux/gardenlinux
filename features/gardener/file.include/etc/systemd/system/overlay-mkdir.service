[Unit]
Requires=overlay.mount
After=overlay.mount
Before=local-fs.target
DefaultDependencies=no

[Service]
Type=oneshot
ExecStart=/usr/bin/mkdir /overlay/upperdir /overlay/workdir

[Install]
WantedBy=local-fs.target
