description: "CIS Compliance"
type: element
features:
  include:
    - cis_packages
    - cis_partition 
    - firewall
    - cis_sysctl
    - cis_os
    - cis_audit
    - cis_sshd
    - cis_modprobe
    - aide
