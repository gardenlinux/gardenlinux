description: "CIS Framework"
type: element
features:
  include:
    - aide
    - cis_audit
    - cis_modprobe
    - cis_os
    - cis_packages
    - cis_partition 
    - cis_sshd
    - cis_sysctl
    - firewall
