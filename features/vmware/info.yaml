description: "VMware platform (cloud-init, open-vm-tools)"
type: platform
features:
  include: 
    - cloud
    - _ignite
disk:
  boot: 
    - mbr
convert:
  format: 
    - type: ova
      template: gardenlinux.ovf.template
