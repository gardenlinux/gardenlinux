#!/usr/bin/env bash
set -Eeuo pipefail

if [[ -f /etc/kernel/cmdline.d/50-ignition.cfg ]]; then rm /etc/kernel/cmdline.d/50-ignition.cfg; fi
if [[ -f /etc/dracut.conf.d/30-ignition.conf ]]; then rm /etc/dracut.conf.d/30-ignition.conf; fi