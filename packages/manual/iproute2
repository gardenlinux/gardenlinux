#!/usr/bin/env bash
set -Eexuo pipefail

. $(dirname $0)/.helper

apt-get source $src

cd $src-*/
patch -p1 < $srcDir/libdb.patch
dch -lgardenlinux 'remove arpd/libdb'
dch -r ''

install_build_deps
donotmove="$(ls ..)"
build
link_version
move_package .. $donotmove
