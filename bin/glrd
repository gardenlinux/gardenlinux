#!/usr/bin/env bash
cmd=$(basename "${0}")
args="$*"
if [ -z "${GARDENLINUX_BUILD_CRE}" ]; then
    GARDENLINUX_BUILD_CRE="sudo podman"
fi
${GARDENLINUX_BUILD_CRE} run -it --rm --pull always -q ghcr.io/gardenlinux/glrd "${cmd} ${args}"
